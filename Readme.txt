The code is in the directory "code".
You should first run requirements.py which is in the main dir to download the libraries
You can run main.py to determine the number of wires and their catenary equation of a batch of data from one of the files in data.

The doc for this package is the Doc.pdf file. The doc file also contains ways for improving.